# Real-time Inference Engine Requirements
# Core dependencies for high-performance fruit detection and label placement

# Essential packages
numpy>=1.21.0
opencv-python>=4.5.0
pillow>=8.0.0
pyyaml>=6.0
flask>=2.0.0

# Computer vision and ML
scikit-image>=0.19.0
scipy>=1.7.0

# GPU acceleration (optional but recommended)
torch>=1.12.0; sys_platform != "darwin"
torchvision>=0.13.0; sys_platform != "darwin"
torchaudio>=0.12.0; sys_platform != "darwin"

# For Apple Silicon Macs
torch>=1.12.0; sys_platform == "darwin"
torchvision>=0.13.0; sys_platform == "darwin"

# ONNX Runtime for cross-platform inference
onnxruntime>=1.12.0
onnxruntime-gpu>=1.12.0; sys_platform != "darwin"

# TensorRT (NVIDIA only, optional)
# tensorrt>=8.4.0; platform_system == "Linux" and platform_machine == "x86_64"

# Performance monitoring
psutil>=5.8.0
memory-profiler>=0.60.0

# Async processing
asyncio-mqtt>=0.11.0
aiofiles>=0.8.0

# Development and debugging
pytest>=7.0.0
pytest-asyncio>=0.20.0
pytest-benchmark>=4.0.0

# Visualization and analysis
matplotlib>=3.5.0
seaborn>=0.11.0

# Production deployment
gunicorn>=20.1.0
uvicorn>=0.18.0
fastapi>=0.85.0

# Hardware optimization libraries
# Intel MKL (Intel CPUs)
mkl>=2022.0; platform_machine == "x86_64"

# OpenVINO (Intel optimization toolkit, optional)
# openvino>=2022.3.0; platform_machine == "x86_64"

# ARM/Mobile optimizations
# tflite-runtime>=2.10.0; platform_machine == "aarch64"

# Edge deployment
# jetson-stats>=4.1.0; platform_machine == "aarch64"

# Additional utilities
tqdm>=4.64.0
colorama>=0.4.4
rich>=12.0.0

# Configuration management
hydra-core>=1.2.0
omegaconf>=2.2.0

# Database and caching (for production)
redis>=4.3.0
sqlalchemy>=1.4.0

# Security
cryptography>=38.0.0